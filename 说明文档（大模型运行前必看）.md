# 说明文档（大模型运行前必看）—— 租卡主机验收 + W3/B2 实验清单

> 适用场景：在 vast.ai 等平台租用 4×GPU 主机，跑 Ferry 的 W3（4 卡矩阵/扩展效率）
> 与 B2（成本感知批粒度，论文新颖性落点）。
> 原则：**先验收拓扑与环境，再出数。验收不过 → 退租换机，别在错环境上跑任何实验。**
> 三关（拓扑）+ 第 4 关（环境稳定性）全绿才进入正式实验。

---

## 0. 这台机器要满足什么条件

| 条件 | 为什么 |
|---|---|
| 4×GPU，**无 NVLink、无 P2P** | 论文前提 = host-staged（跨卡必经 host 内存）。有 P2P 整个前提不成立 |
| 4 卡全部 **x16 直连 CPU**（拓扑显示 PHB/SYS/NODE 均可，不能是 PXB/PIX 挂 switch 的 x4） | x4 挂载会把 host-staged 带宽砍到不可比 |
| 卡型优先 **4×3090 / 3090Ti**（sm_86） | 与 2 卡主数字（集群 3090）同代，扩展效率曲线直接可比 |
| 租法：**Datacenter 认证主机 + On-Demand** | 矩阵一跑几小时，Interruptible 会被抢；认证主机拓扑可信 |

---

## 1. 环境准备（vast.ai PyTorch 镜像一般自带 nvcc/gcc，缺啥补啥）

```bash
# CUDA toolkit + 编译器 + cmake（镜像缺哪个装哪个）
which nvcc || echo "换带 CUDA 的镜像，别手装 toolkit"
which cmake || (apt update && apt install -y cmake git)

# 取代码（二选一）
git clone https://github.com/clearleo126/ferry && cd ferry   # 若为私有仓库，用带 token 的 URL 或改用下面打包上传
# 本机打包上传：  tar czf ferry.tgz --exclude=build --exclude=.git ferry && scp ferry.tgz root@<ip>:/root/

# 构建（3090=sm_86；native 自动识别本机）
cmake -S . -B build -DCMAKE_CUDA_ARCHITECTURES=native && cmake --build build -j8
```

## 2. 拓扑验收（三关全绿才继续）

### 第 1 关：互联矩阵 + 逐对 host-staged 吞吐（一条命令搞定）

```bash
nvidia-smi topo -m     # 先肉眼确认互联；再跑 probe 拿带宽
./build/probe          # 完整拓扑报告 + 逐对 host-staged 吞吐（P0 工具）
```

- 4 卡两两之间只允许 `PHB` / `NODE` / `SYS`；**出现任何 `NV#` = 有 NVLink，退租**
- 看 4 张卡的 `PCIe Affinity`：理想是分散在 2 个 NUMA 节点（如 0-15,32-47 / 16-31,48-63）；全部挤在同一组也行，但记录下来
- `./build/probe` 的逐对吞吐表是**跨卡实测**，直接和集群数字比

### 第 2 关：P2P 必须关闭

```bash
python3 - <<'EOF'
import ctypes
rt = ctypes.CDLL("libcudart.so")
for a in range(4):
    for b in range(4):
        if a == b: continue
        v = ctypes.c_int(-1)
        rt.cudaDeviceCanAccessPeer(ctypes.byref(v), a, b)
        print(f"GPU{a}->GPU{b}: {v.value}")
EOF
```

- **全部必须为 0**。任何 1 = P2P 可用，退租（4090 系消费卡一般禁 P2P，3090 系如果主机开了 IOMMU 豁免可能出 1，务必查）

### 第 3 关：通道带宽复测（两个工具，互补）

```bash
# (a) 环回：channel（独立 H2D 汇聚流 + event）对比 p1-batched（单流交替 D2H→H2D）
#     基准：channel ≈19 GB/s、p1 ≈10 GB/s，比值 ≈1.85x（这是 M1 的来源）
./build/bench_comm

# (b) 跨卡：首尾两卡之间的粒度扫描（单流 vs 批，覆盖 16KB~16MB）
#     基准：≥1MB 段饱和，单流 ≈11-12 GB/s
./build/probe --bw
```

**判定（对齐集群 4×3090 实测）**：
- bench_comm 的 `ratio` 列应 ≈1.85x，且带 `(PASS)` 标记
- probe --bw 的单流列在 ≥1MB 后应≈11–12 GB/s 并趋平

**偏差 >20% 或量级不对 → 这台机器的 PCIe 平台和集群差异太大，数字不可混用，退租换机。**

> 保存验收记录：`nvidia-smi topo -m > topo.txt; ./build/probe > probe.txt; ./build/bench_comm > bw.txt`
> 注意：`bench_comm` 用当前设备（默认 0），`probe --bw` 固定用**首卡→尾卡**这一对；4 卡机器上都是跨卡，可接受。

### 第 4 关：环境稳定性自检（重复性 + 同机邻居争用）

> **背景（2026-09-23 实测发现，务必先读）**：在自有共享集群上，当 **GPU0/1 被他人 vLLM
> 占用（util=100%）**时，GPU2/3 的重复测量呈**双峰**——快模 ≈215–225ms、慢模 ≈250–272ms
> （差 15–25%），且**与自身配置无关**（`--sat-mb=4.0` 参数未变，前 5 次全快模、后 2 次全转慢模）。
> 机制：若邻居为 TP≥2，其每 token 的 all-reduce 走 PCIe + host 通路，**与本系统
> host-staged 迁移争用同一 PCIe/内存带宽**。这正是集群 std 高达 18–29%、而
> vast 独占机 std 仅 1–8% 的原因。
>
> 后果：**单次跑的 A/B 差异 <25% 完全不可信**；B2 的效应量只有 ~5%，
> 在有邻居争用的机器上会被直接淹没。

```bash
# (a) 邻居检查：确认没有别的进程在占卡/占 PCIe
nvidia-smi                       # 期望：4 张卡 util≈0、显存只剩你自己的
nvidia-smi --query-compute-apps=pid,used_memory --format=csv   # 期望：只有你的进程

# (b) 重复性自检：同一配置连跑 7 次，看离散度
for rep in 1 2 3 4 5 6 7; do
  ./build/run_multigpu --gpus 0,1,2,3 --tasks 32768 --arrival steady --skew imbalanced \
    2>&1 | grep -oE "ferry-C +makespan= *[0-9.]+ms"
done
```

**判定**：
- 7 次 C 的 makespan **std / mean < 10%** → 环境干净，可进入正式实验
- **std / mean ≥ 10%，或明显呈双峰（如 ~218 与 ~250 两簇）** → 环境被争用。
  处置：① 等邻居空闲窗口重测；② 或换独占机（vast）；③ 若无法避免，
  则在论文中声明"共享集群 + 同机 PCIe 争用"，并把重复次数提到 10+ 次报均值±std。

## 3. 冒烟测试（验收通过后，先小规模确认代码能跑完）

```bash
# 2 卡冒烟（任选两卡），32768 任务，三个模式都要完成、无死锁
timeout 300 ./build/run_multigpu --gpus 0,1 --tasks 32768 --arrival steady --skew balanced
# 期望末行形如： => C/A ~1.5x  C/B ~1.9x  steals(B/C) ...（无 [B]/[C] idle heartbeat 刷屏）
# 4 卡冒烟
timeout 600 ./build/run_multigpu --gpus 0,1,2,3 --tasks 65536 --arrival steady --skew imbalanced
```

## 4. W3 正式矩阵（结果统一落 results/，全部跑完再关机）

```bash
mkdir -p results/w3
# 4.1 负载均衡 × 到达模式（4 卡，32768 任务，与集群 W2 同口径）
for sk in balanced imbalanced; do
  for ar in steady bursty skewed; do
    ./build/run_multigpu --gpus 0,1,2,3 --tasks 32768 --arrival $ar --skew $sk \
      | tee results/w3/mg_${ar}_${sk}.log
  done
done
# 4.2 trace 到达（真实 Mooncake 到达序列；csv 已入库，不需要原始 jsonl）
for tr in arxiv toolagent; do
  FERRY_TRACE_CSV=data/${tr}_arrivals.csv \
    ./build/run_multigpu --gpus 0,1,2,3 --tasks 33000 --arrival trace --skew imbalanced \
    | tee results/w3/mg_trace_${tr}.log
done
# 4.3 消融（2 卡代表 + 4 卡各一条，确认 M1×M3 协同在 4 卡仍在）
./build/run_ablation --src 0 --dst 1 --arrival steady --sparsity medium --tasks 32768 \
  --csv results/w3/ablation_x2.csv | tee results/w3/ablation_x2.log
./build/run_ablation --src 0 --dst 2 --arrival steady --sparsity medium --tasks 32768 \
  --csv results/w3/ablation_x4.csv | tee results/w3/ablation_x4.log
```

# 4.4 B1/B2 公平对照（与 4.1 同命令，输出自动多出）
```bash
# 任何 run_multigpu 命令现在固定输出 6 行：static-A / dyn-B / ferry-C /
# one-steal-D1 / half-steal-D2 / costaware-E，末尾多两行汇总：
#   [B1]（窃取粒度）：D1 = 每次偷 1 个（B 的粒度，保留 C 的异步流水）；
#        D2 = 每次偷 victim 一半（StealHalf 粒度）
#   [B2]（批大小决策）：E 与 C 同引擎同窃取语义，唯一差异 = 批大小——
#        C 用任务总量常量；E 按积压/源剩余/在途窗口/通道饱和区实时决策
#        （--sat-mb 可调饱和点，默认 1MB）
# 4.1 的六格矩阵命令照抄即可，[B1]/[B2] 行自动带出：
./build/run_multigpu --gpus 0,1,2,3 --tasks 32768 --arrival steady --skew imbalanced \
  | tee results/w3/b1b2_steady_imb.log
```

# 4.5 B2 四卡验证（重租机器时的专项——论文新颖性落点，优先级最高）
```bash
# E 的动态决策只在浅队列/多卡争用下有区分度（单卡深队列时 E≈C，已实测）。
# 判据：C/E ≥ 1.05x 且 E 的 p99 显著低于 C；否则如实记录"无增益"。
# ⚠️ 重复次数用 7 次而非 5 次：B2 效应量仅 ~5%，而噪声可达 10–25%（见 §2 第 4 关），
#    2 卡集群实测 C/E = 0.986±0.066（bursty 0.952±0.107）→ 5 次不足以判定。
mkdir -p results/w3/b2
for rep in 1 2 3 4 5 6 7; do
  for ar in steady bursty skewed; do
    ./build/run_multigpu --gpus 0,1,2,3 --tasks 32768 --arrival $ar --skew imbalanced \
      | tee results/w3/b2/b2_${ar}_r${rep}.log
  done
done
# P1（模型预测 g_opt ≈ g*）专项：--sat-mb 扫 5 档 × 7 次
#   判据：makespan 应在 g*≈1MB 附近出现内部最优；若单调或全平 → P1 不成立
for s in 0.25 0.5 1.0 2.0 4.0; do for rep in 1 2 3 4 5 6 7; do
  ./build/run_multigpu --gpus 0,1,2,3 --tasks 32768 --arrival steady --skew imbalanced \
    --sat-mb $s | tee results/w3/b2/p1_sat${s}_r${rep}.log
done; done
# 汇总（看 [B2] 行的 C/E 与 mean batch 列）：
grep -h "\[B2\]" results/w3/b2/b2_*.log
grep -hoE "costaware-E +makespan= *[0-9.]+ms" results/w3/b2/p1_sat*.log
```

> **已排除的两项（不必重做）**：① B1 粒度对照已在 2 卡独立复现（C/D1=1.14±0.08、
> C/D2=1.00±0.07），与 vast 4 卡一致 → 只需在 4 卡再取一次即可；② B2 在 2 卡上
> E≈C（无增益），故 4 卡是唯一判定场合。详见 `results/a5_b2_2card_20260923.md`。

## 5. 结果回收（关机前！）

```bash
tar czf w3_results.tgz results/w3 topo.txt bw.txt
# scp w3_results.tgz 回本机；或直接 cat 各 log 贴回
```

**按小时计费，`results/` 没拉下来千万别销毁实例。**

## 6. 常见坑（每条都踩过）

1. **`./probe: No such file or directory`** → 没构建，先在仓库根目录跑 §1 的 cmake 两连；probe/bench_comm 都在 `./build/` 下（不是仓库根目录直接跑）。
2. **CUDA arch 不匹配** → 3090=sm_86。CMake 默认 `native` 即可；报 arch 错误就显式 `-DCMAKE_CUDA_ARCHITECTURES=86`。
3. **驱动版本太老带不动新 CUDA** → 本仓库 C++17 + 基本 CUDA API，要求极低；若镜像 CUDA < 11.4 换镜像。
4. **多用户机器别人占卡** → vast On-Demand 是独占的；但自有共享集群**必须查邻居进程**：
   若 GPU0/1 有他人 vLLM 在跑（util>0），会与本系统争 PCIe/内存带宽，造成 15–25% 的
   双峰噪声（见 §2 第 4 关）。此时**单次 A/B 差异不可信**，需 7+ 次重复或换独占机。
5. **trace 模式报 "csv 行数不足" 正常退出（exit 2）** → 检查 `FERRY_TRACE_CSV` 路径与 `--tasks` 数量匹配（csv 33000+ 行）。
6. **数字与集群差异 >20%** → 先看第 3 关的 bw.txt：多半是主机 PCIe 平台不同（slot 代数/CPU NUMA），不是代码问题。数字不可混用，论文里注明硬件。

## 7. 对齐清单（写进实验记录）

- [ ] topo -m 截图/文本（无 NV#）
- [ ] P2P 16 组查询全 0
- [ ] bench_comm 环回 GB/s（channel / p1 / 比值）
- [ ] **环境稳定性：邻居检查干净 + 7 次重复 std/mean < 10%（§2 第 4 关）**
- [ ] 2 卡 + 4 卡冒烟通过（A/B/C 全完成，含 D1/D2/E 共 6 行）
- [ ] 4.1–4.3 全部 log + csv 已回传
- [ ] **4.5 B2 专项 + P1 饱和点扫描（7 次/档）已回传**
